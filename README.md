# URLShortener
- Create a basic URL Shortening Service using C++. - The application should allow users to enter a long URL, generate a shortened URL, and retrieve the original URL from the shortened version.
